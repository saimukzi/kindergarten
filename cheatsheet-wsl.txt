Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# https://www.tensorflow.org/install/pip#windows-wsl2_1

# Install WSL2
# https://docs.microsoft.com/windows/wsl/install
# Use Ubuntu default

# Setup NVIDIA® GPU support in WSL2
# https://docs.nvidia.com/cuda/wsl-user-guide/index.html

# https://www.tensorflow.org/install/pip#windows-native_1

# https://developer.nvidia.com/cuda-downloads?target_os=Linux&target_arch=x86_64&Distribution=WSL-Ubuntu&target_version=2.0&target_type=deb_network
# Linux > x86_64 > WSL-Ubuntu > 2.0 > deb (network)

# install cudnn


===

# Windows app env

# DL miniconda https://docs.conda.io/en/latest/miniconda.html
# Better install menu item

conda create --name gamebot-env python=3.10
conda deactivate
conda activate gamebot-env

python -m pip install --upgrade pip
pip install tensorflow-cpu
pip install wmi

# verify tensorflow
python -c "import tensorflow as tf; print(tf.reduce_sum(tf.random.normal([1000, 1000])))"

# verify GPU
python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"

pip install numpy opencv-python pillow pygetwindow
# pip install pyautogui 
pip install mss

===

python -m venv venv
venv\Scripts\Activate.ps1
python -m pip install --upgrade pip wheel
pip install tensorflow

python otm_mogura.py
